public class Tablice {

  public static void main(String args[]) {


    /*
    int[] numbers = {1, 2, 3, 4, 5};
    int first = numbers[0];
    int last = numbers[numbers.length - 1];

    numbers[0] = numbers[numbers.length - 1];
    numbers[numbers.length - 1] = numbers[0];
    for (int number : numbers) {
      System.out.println(number);
    }

    int[] nums = {0, 5, 3, 7, 4, 5, 5, 9, 11, 45};
    int parz = 0;
    int nieparz = 0;
    for (int num : nums) {
      if (num % 2 == 0) {
        parz++;
      } else {
        nieparz++;
      }
    }
    System.out.println("Parzystych jest " + parz);
    System.out.println("Nieparz jest " + nieparz);

    int[] numbers = {5, 2, 7, 4, 9, 0, 8, 1, 6};
    int min;
    for (int i = 0; i < numbers.length; i++) {
      min = numbers[i];
      for (int j = i + 1; j < numbers.length; j++) {
        if (numbers[j] < min) {
          min = numbers[j];
          numbers[j] = numbers[i];
          numbers[i] = min;
        }
      }
    }

    for (int num:numbers) {
      System.out.println(num);
    }


    int max;
    for (int i = numbers.length-1; i < numbers.length; i--) {
      max = numbers[i];
      for (int j = i - 1; j < numbers.length-1; j--) {
        if (numbers[j] > max) {
          max = numbers[j];
          numbers[j] = numbers[i];
          numbers[i] = max;
        }
      }
    }

    System.out.println("Sortowanie w druga strone");

    for (int num:numbers) {
      System.out.println(num);
    }
*/
  }
}
